
- **02:06 (cron: network-alert-check)**: 偵測到網路異動報告 — 埠口變動（192.60.1.142 移除 23/tcp telnet）。Telegram bot 仍被封鎖/踢出頻道（chat_id=-1002070752124），無法發送通知。已將 `sent` 標記為 `true` 避免重複嘗試。

## 系統環境補強
- 安裝 agent-browser（Chrome 需 `--no-sandbox` 模式，VM 缺乏 user namespaces）
- 用 setup.sh 透過 sudo 裝了 ffmpeg、gh、jq
- npm global 補了 prettier、pm2
- 設定 AGENT_BROWSER_ARGS="--no-sandbox" 到 bashrc
# 2026-06-14 週日

## 🎋 抽籤技能開發

### 建立 `/draw` 技能（lot-draw）
- 從台灣好廟網 `temples.tw` API 抓取完整 60 首六十甲子籤詩文
- 從 stellarnier.com 抓取 60 首完整白話解籤說明
- 資料存於 `memory/sixty_lots.json`（籤詩原文）和 `memory/sixty_lots_interpret.json`（白話解說）
- 技能指令 `/draw`，隨機抽 1~60 籤，即時回覆完整籤詩 + 解說
- 預設展示方式由濃縮版改為**完整版**（列全部解說句）

## 💻 系統環境整理

### 已安裝工具
| 工具 | 來源 | 用途 |
|------|------|------|
| **agent-browser** | npm global (`--no-sandbox`) | 瀏覽器自動化 |
| **prettier** | npm global | 程式碼格式化 |
| **pm2** | npm global | 背景進程管理 |
| **eslint** | npm global | JS/TS 程式碼檢查 |
| **ffmpeg** | apt | 音訊/影片處理 |
| **gh (GitHub CLI)** | apt | GitHub 操作 |
| **jq** | apt | JSON 解析 |
| **fonts-noto-emoji/cjk** | apt | 中日韓/表情字型 |

### 已知限制
- Chrome sandbox 不可用（VM 無 user namespaces），agent-browser 需 `--no-sandbox` 模式，已設 bashrc alias
- Google 搜尋會擋 headless Chrome，改用 SearXNG、DuckDuckGo 或直接 API
- VM 無 sudo 密碼無法互動，需預寫腳本讓使用者跑
